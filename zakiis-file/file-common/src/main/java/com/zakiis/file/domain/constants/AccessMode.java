package com.zakiis.file.domain.constants;

public enum AccessMode {

	/** public access mode allows every channel read files in this bucket */
	PUBLIC,
	/** private access mode means channel must in access filed of this bucket  */
	PRIVATE,
	;
}
